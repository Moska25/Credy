.venv/
__pycache__/
*.py[cod]
*.db
.pytest_cache/
data/cache/
.DS_Store

# Local design surface. The UI kit, the component gallery and the agent prompts
# live here so they can be worked on next to the app and handed to a designer,
# but they are not part of the product: nothing under app/ imports them and no
# route serves them. Deleting the folder changes zero pages.
design/

# Next.js UI in web/
web/node_modules/
web/.next/
web/next-env.d.ts
web/tsconfig.tsbuildinfo
.vercel
.env*
